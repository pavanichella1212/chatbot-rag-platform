# Copy this file to .env in the project root and fill in real values.
# .env is gitignored and should never be committed.
#
# Nothing in src/main.py reads these yet — this is a placeholder for
# settings the real RAG/chatbot logic will need later.

# TODO: wired up in a future ticket once the RAG pipeline lands
# LLM_API_KEY=
# VECTOR_DB_PATH=
